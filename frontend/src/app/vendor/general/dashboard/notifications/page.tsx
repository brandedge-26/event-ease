"use client";

// This page re-uses the exact same component as the venue dashboard notifications page.
// The API endpoints are identical for both vendor types.
export { default } from "@/app/vendor/dashboard/notifications/page";
